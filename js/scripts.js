// Utility Logic
// Business Logic
function beepBoop(num) {
  numHolderArray = [];
  if (num === 0) {
    numHolderArray.push(num);
  }
  console.log(numHolderArray, "numHolderArray");
}
beepBoop(0);
//UI Logic